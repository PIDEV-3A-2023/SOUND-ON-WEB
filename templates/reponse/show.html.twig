{% extends 'base.html.twig' %}

{% block title %}Reponse{% endblock %}

{% block reclamation %}
    <h1>Reponse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponse.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ reponse.message }}</td>
            </tr>
            <tr>
                <th>DateCreation</th>
                <td>{{ reponse.dateCreation ? reponse.dateCreation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateUpdate</th>
                <td>{{ reponse.dateUpdate ? reponse.dateUpdate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_reponse_index') }}">back to list</a>

    <a href="{{ path('app_reponse_edit', {'id': reponse.id}) }}">edit</a>

    {{ include('reponse/_delete_form.html.twig') }}
{% endblock %}
