{% extends 'base.html.twig' %}

{% block title %}Commande{% endblock %}

{% block body %}
    <h1>Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande.id }}</td>
            </tr>
            <tr>
                <th>Totale</th>
                <td>{{ commande.totale }}</td>
            </tr>
            <tr>
                <th>DateCommande</th>
                <td>{{ commande.dateCommande ? commande.dateCommande|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_front_office_commande_index') }}">back to list</a>

    <a href="{{ path('app_front_office_commande_edit', {'id': commande.id}) }}">edit</a>

    {{ include('front_office/commande/_delete_form.html.twig') }}
{% endblock %}
